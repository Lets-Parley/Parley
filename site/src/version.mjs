// The one place the current release is written down. A release bumps this line
// and the whole site follows: prose, code fences, and the verified stamp.
export const VERSION = "0.7.3";
export const VERIFIED_VERSION = `v${VERSION}`;
