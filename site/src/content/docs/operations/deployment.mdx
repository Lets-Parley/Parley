---
title: Deployment
description: Docker Compose, a bare binary, and the settings that matter in both.
---

import VerifiedStamp from "../../../components/VerifiedStamp.astro";
import Capability from "../../../components/Capability.astro";

<VerifiedStamp version="unreleased" source={["docker-compose.yml", "Dockerfile", ".github/workflows/release.yml", "cmd/parley/main.go"]} />

Three supported shapes: Compose, [Kubernetes](/operations/kubernetes/), or the
binary on its own. All three are the same process with the same configuration —
see the [configuration reference](/reference/configuration/) for every variable.

For a first run, start at the [Quickstart](/quickstart/).

## Docker Compose

The shipped `docker-compose.yml` runs Parley plus a Postgres 16, with a named
volume for the data.

Three of its defaults are deliberate and worth knowing before you change them:

- **The password has no default.** `POSTGRES_PASSWORD` is written
  `${POSTGRES_PASSWORD:?...}`, so Compose refuses to start without one rather
  than shipping a guessable default into production. Self-hosters paste compose
  files verbatim; this one will not let you paste a weak secret you did not
  choose.
- **The port is bound to loopback**, `127.0.0.1:8080:8080`. Nothing is exposed to
  your network until you decide to expose it. Prefer putting a
  [reverse proxy](/operations/reverse-proxy/) in front over changing this to
  `0.0.0.0`.
- **`TRUST_PROXY_HEADERS` defaults to false**, which is correct for the default
  loopback binding. Enable it behind a proxy only with an explicit
  `TRUSTED_PROXY_CIDRS` allowlist. See below.
- **Postgres is pinned to a major version.** An unpinned `postgres:` tag that
  crosses a major release leaves the data directory unreadable. See
  [Upgrading](/operations/upgrading/).

## The image

`ghcr.io/lets-parley/parley`, multi-arch for `linux/amd64` and `linux/arm64`,
published from exact semantic-version GitHub Releases and anonymously pullable.

It is built in three stages and the final one is
`gcr.io/distroless/static-debian12:nonroot` — no shell, no package manager, no
libc to patch, running as a non-root user. The Go binary is static
(`CGO_ENABLED=0`).

The image declares its own `HEALTHCHECK`, which runs `parley -healthcheck`
against `/readyz` over loopback every 15 seconds.

Pin a version rather than tracking `latest` if you want reproducible deploys.
Beginning with the first release produced by the hardened workflow, release
assets include digest-qualified Compose and Kubernetes manifests, an SPDX SBOM,
and verifiable provenance for the published digest. Releases v0.1.0 through
v0.2.1 were not backfilled; see [Supply chain](/security/supply-chain/).

## The bare binary

`go build ./cmd/parley` produces a single file with the frontend inside it. Give
it a `DATABASE_URL` and it runs.

:::caution[There is no bind-address setting]
`PORT` sets the port; the process always listens on **all interfaces**. If you
run the bare binary on a multi-homed host and want it reachable only on
loopback, you need a firewall rule or a proxy — Parley cannot be told to bind
`127.0.0.1`.
:::

## Getting TRUST_PROXY_HEADERS right

This setting is wrong in both directions and both failures are real.

- **A client-reachable network is trusted** — a caller can supply a forged
  `X-Forwarded-For`, defeating address-based identity and room-code controls.
- **Behind a proxy, set to `false`** — every request appears to come from the
  proxy, so the whole internet shares one throttle bucket. Eight wrong guesses
  by anyone locks everyone out of that space.

Set it to `true` if and only if a proxy you control sets `X-Forwarded-For`
itself. `TRUSTED_PROXY_CIDRS` is then required and must list every immediate and
intermediate proxy hop, never client networks. Parley walks the chain
right-to-left, ignores malformed headers and headers from untrusted immediate
peers, and uses the first untrusted address. Both shipped manifests default
proxy trust to `false`.

## Public exposure

`AUTH_MODE=open` is for a trusted network. A public deployment needs a space
passcode or an external SSO/authentication proxy, plus ingress controls for
request rate, concurrent connections, and bandwidth. Built-in quotas and
identity-creation throttles limit persistent growth but do not make anonymous
public access an authentication boundary.

## Before you expose it

<Capability rows={[
  { name: "TLS", status: "not-built", detail: "Parley never terminates TLS. Put <a href='/operations/reverse-proxy/'>Caddy or nginx</a> in front — and it must be TLS, because <code>BASE_URL</code> being https is what sets the session cookie's Secure flag." },
  { name: "BASE_URL matching reality", status: "built", detail: "It drives the WebSocket origin check, the cookie Secure flag and the OIDC redirect. Getting it wrong is the most common deployment failure." },
  { name: "Database backups", status: "not-built", detail: "Nothing in Parley backs itself up. Set up <a href='/operations/backups-and-recovery/'>pg_dump</a> before you have data worth keeping." },
  { name: "Resource limits", status: "not-built", detail: "The k8s manifest sets requests but no limits, on purpose. Add them once you have measured your own usage." },
  { name: "Horizontal scaling", status: "built", detail: "Fanout, presence and the room-code throttle all go through Postgres, and simultaneous boots serialize their migrations, so replicas can be added. Size <code>max_connections</code> for <code>replicas &times; 11</code>. See <a href='/operations/scaling-and-limits/'>Scaling and limits</a>." },
  { name: "Public access control", status: "not-built", detail: "Open mode is trusted-network-only. Public instances need a passcode or external SSO/auth proxy plus ingress abuse controls." },
]} />

Then walk the [hardening checklist](/security/hardening-checklist/).
