---
title: Daily standup
description: Round-robin with a per-person timer, carry-forward, and a blockers roundup.
---

import VerifiedStamp from "../../../components/VerifiedStamp.astro";
import Capability from "../../../components/Capability.astro";
import Screenshot from "../../../components/Screenshot.astro";
import roundLight from "../../../assets/standup-round-light.png";
import roundDark from "../../../assets/standup-round-dark.png";
import blockersLight from "../../../assets/blockers-roundup-light.png";
import blockersDark from "../../../assets/blockers-roundup-dark.png";

<VerifiedStamp source={["internal/standup/state.go", "internal/standup/routes.go", "web/src/pages/StandupRoom.tsx"]} />

A standup session is three text fields per person — yesterday, today, blockers —
and an order to speak in.

## Writing ahead

Everyone can fill their entry in before the standup starts, and most people
should. Entries autosave as you type, roughly every 800ms, with a visible
saving/saved indicator.

The draft is deliberately **never rehydrated from an incoming broadcast**. Parley
replaces the whole session state on every update, and an earlier version of this
screen used that state to populate the textarea — which meant somebody else's
edit could land mid-sentence and eat your keystrokes. Your own field is local
until you stop typing.

Each field is capped at 2000 characters.

## Starting the round

When the facilitator starts the standup, the server takes a snapshot of who is
currently connected and not spectating, and that becomes the speaking order for
the session. People who join later do not get spliced into an order that is
already underway.

Anyone who has not written anything yet is placed in name order. Anyone who
filled their update in **before** the round started keeps the position they got
when they saved it, which is the order they saved in — so a round where people
wrote ahead is not alphabetical.

At the same moment, **yesterday writes itself**: each person's "today" from the
previous standup in that space is copied into their "yesterday". It is a draft,
not a commitment — edit it or delete it.

## Running the round

The speaker rail shows who is up, who has been, and who was skipped. The
facilitator advances with **Next**, or marks someone **absent** with Skip, which
strikes them through and moves on.

There is a per-person countdown, driven off the server's clock rather than the
browser's, so everyone in the room sees the same number.

Clicking anyone in the rail opens their yesterday/today/blockers, so an update
you missed is a click away rather than gone once the turn passes. The panel
follows whoever is speaking until you pick somebody, and then stays put — a turn
change will not pull you out of the update you are reading. Everyone stays
clickable after the round finishes, skipped people included.

<Screenshot
  light={roundLight}
  dark={roundDark}
  alt="A standup in progress: the countdown is running, the speaker rail lists six people with one struck through as absent and the current speaker highlighted, and that person's yesterday, today and blockers are shown below."
  caption="The rail carries the whole order at once — done, skipped, and up next — while the countdown runs for whoever is speaking."
/>

## Ending the standup

The facilitator has an **End session** control in the header. It closes the
session the same way poker's does: the room stops being live, and the entries
and the blockers roundup stay readable in the space afterwards.

## The blockers roundup

Anything anybody typed into "blockers" collects into a single list at the end,
with a copy-to-clipboard button — which is the bit that actually gets pasted into
a channel afterwards.

<Screenshot
  light={blockersLight}
  dark={blockersDark}
  alt="The blockers roundup at the end of a standup: three lines, each naming a person and the blocker they wrote, above a Copy blockers button."
  caption="Only the people who wrote a blocker appear. The text is already formatted for the channel it is about to be pasted into."
/>

## What is not here

<Capability rows={[
  { name: "Configurable timer length", status: "not-built", detail: "The server supports a <code>secondsPerPerson</code> session setting and defaults it to 90, but nothing in the UI sets it. Treat 90 seconds as fixed." },
  { name: "Async standup", status: "not-built", detail: "Write ahead of time and let the round run without you; a scheduled digest is on the <a href='/project/roadmap/'>roadmap</a>." },
  { name: "Reordering the speaking order", status: "not-built", detail: "Fixed once the round starts. Skip moves past anyone who is not ready." },
  { name: "Facilitator-offline claim", status: "not-built", detail: "The endpoint is kind-agnostic but the 60-second claim banner is only wired into the poker room. End and recreate the standup if its facilitator vanishes." },
  { name: "Chasing people who did not write anything", status: "out-of-scope", detail: "Parley has no notification channel and does not want one." },
]} />
