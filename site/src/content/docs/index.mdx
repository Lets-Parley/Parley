---
title: Parley
description: Self-hosted planning poker and daily standups. No accounts, no seat count — just a link your team follows. One Go binary and a Postgres database.
template: splash
hero:
  tagline: No accounts, no seat count, no credit card — just a link your team follows. One Go binary and a Postgres database.
  image:
    alt: A revealed planning poker round in Parley
    file: ../../assets/poker.png
  actions:
    - text: Get it running
      link: /quickstart/
      icon: right-arrow
    - text: Read the threat model
      link: /security/threat-model/
      variant: minimal
---

import { CardGrid, Card, LinkCard, Tabs, TabItem } from "@astrojs/starlight/components";
import Screenshot from "../../components/Screenshot.astro";
import hiddenLight from "../../assets/hidden-votes-light.png";
import hiddenDark from "../../assets/hidden-votes-dark.png";
import tshirtLight from "../../assets/tshirt-results-light.png";
import tshirtDark from "../../assets/tshirt-results-dark.png";
import standupLight from "../../assets/standup-round-light.png";
import standupDark from "../../assets/standup-round-dark.png";
import spaceLight from "../../assets/space-room-code-light.png";
import spaceDark from "../../assets/space-room-code-dark.png";
import storyLight from "../../assets/story-queue-light.png";
import storyDark from "../../assets/story-queue-dark.png";

## What a round looks like

Everyone votes at once, and nobody sees a number until the round opens. Your
teammates don't install anything and don't sign up for anything — they open the
link and type a name.

<Screenshot
  light={hiddenLight}
  dark={hiddenDark}
  alt="Four face-down cards at the table and two empty seats, with the line '4 of 6 voted' underneath and the viewer's own 5 raised in their hand."
  caption="Mid-round: the count reads 4 of 6, and the only value anyone can see is the one in their own hand."
/>

<Screenshot
  light={tshirtLight}
  dark={tshirtDark}
  alt="A revealed T-shirt round showing a large M under the word MODE, the line 'range S–L · 6 votes · ordinal deck, no average', and counts of S times one, M times four, L times one."
  caption="Revealed. A T-shirt round reports mode and range, because averaging sizes gives you a number nobody can act on."
/>

## Most tools want an account. Parley wants a link.

Same link for planning poker and standup. Two commands and a `.env` file, no
platform team required:

<Tabs>
  <TabItem label="Compose">
    ```sh
    git clone https://github.com/lets-parley/parley && cd parley
    cp .env.example .env        # set POSTGRES_PASSWORD to anything
    docker compose up -d
    ```
    That is the whole install, and it is on `:8080`. Compose binds to localhost
    until you say otherwise.
  </TabItem>
  <TabItem label="One server">
    Compose, a domain, and a reverse proxy in front — one box your whole team
    points at, no cluster. Parley runs as a single replica by design, so this is
    the shape it is built for, and a small cloud instance carries a team of a
    few dozen. [One server →](/operations/single-server/)
  </TabItem>
  <TabItem label="Prebuilt image">
    ```sh
    docker run -d --name parley -p 8080:8080 \
      -e DATABASE_URL='postgres://parley:secret@db:5432/parley' \
      -e BASE_URL='https://parley.example.com' \
      ghcr.io/lets-parley/parley:v0.2.2
    ```
    Multi-arch, amd64 and arm64. Bring your own Postgres. Run v0.2.2 or later:
    every earlier release can be crashed remotely by a disconnecting client, and
    v0.1.0 additionally has a room-code throttle bypass. None were retagged onto
    fixed code.
  </TabItem>
  <TabItem label="Kubernetes">
    Manifests live in `deploy/k8s/` — a Deployment pinned to one replica, a
    Service, and a commented Ingress carrying the read-timeout annotations
    WebSockets need. [Deploying to Kubernetes →](/operations/kubernetes/)
  </TabItem>
</Tabs>

The frontend ships inside the binary, so there is no second service, no Node
runtime in production, and no way for the two halves to end up at different
versions. Migrations run themselves at boot.

## What's in it

<CardGrid>
  <Card title="Planning poker">
    A story queue with real ticket references. Four decks — Fibonacci, modified
    Fibonacci, T-shirt, powers of two — each carrying `?` and a coffee card.
    <Screenshot
      light={storyLight}
      dark={storyDark}
      alt="A poker room with ticket PLAT-412 on the table alongside its note, and a queue of five more items listed to the right."
    />
    [What it does →](/features/planning-poker/)
  </Card>
  <Card title="Daily standup">
    Round-robin with a 90-second clock, so the quiet people get their turn.
    Whatever you wrote under "today" is waiting under "yesterday" tomorrow, and
    blockers collect into a list you can paste into a channel.
    <Screenshot
      light={standupLight}
      dark={standupDark}
      alt="A standup in progress: a countdown reading 1:08, a rail of six speakers with one struck through, and the current speaker's yesterday, today and blockers fields."
    />
    [What it does →](/features/daily-standup/)
  </Card>
  <Card title="Spaces">
    One memorable link per team, `/s/platform-team`, and a six-character room
    code that throttles wrong guesses per address. The roster shows who is here
    and which table they are sitting at.
    <Screenshot
      light={spaceLight}
      dark={spaceDark}
      alt="A space page for Platform Team: a sidebar of sessions and six members with presence dots, recent sessions in the middle, and a panel showing the passcode TEAM49 with Copy, New code and Make open buttons."
    />
    [What it does →](/features/spaces-and-room-codes/)
  </Card>
</CardGrid>

## What Parley doesn't do

- **One replica, and it enforces that.** A second instance refuses to start
  rather than serve a divergent board. Size it vertically; deploy when nobody
  is mid-session.
- **No roles beyond member and facilitator.** Any member can rotate the room
  code or open the space. Keep membership deliberate, or put an SSO proxy in
  front of it.
- **No metrics, tracing, SBOM or image signing.** Structured JSON logs to
  stdout are all it emits; build from a tagged commit yourself if you need
  provenance.
- **Turning on OIDC signs everyone out.** Anonymous accounts are not linked to
  federated ones. Switch on an instance whose history you are willing to leave
  behind.

All of them are on one page, each with its workaround, because the gaps
are the reason to believe the rest.

[Known limitations →](/known-limitations/)

## In production

- A Postgres blip does not restart the container and drop every socket in the
  room — the health check never touches the database
- The container is distroless and non-root, no shell, one static binary —
  amd64 and arm64
- CSV exports cannot run formulas in your spreadsheet, and never contain a vote
  you were not allowed to see

<CardGrid>
  <LinkCard
    title="Operations"
    href="/operations/"
    description="Architecture, deployment on one box or a cluster, backups, upgrades, and a runbook keyed by symptom."
  />
  <LinkCard
    title="Security"
    href="/security/"
    description="Threat model, authentication, authorization, data and privacy, and a single-page review pack."
  />
</CardGrid>

<p style="text-align:center; margin-top:2.5rem;">
  [Start here →](/quickstart/)
</p>
